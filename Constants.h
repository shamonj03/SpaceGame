#pragma once

#include <GL/glew.h>
#include <SDL_main.h>
#include <SDL.h>
#include <SDL_opengl.h>
#include <glm/vec3.hpp>

static const GLint WIDTH = 800;
static const GLint HEIGHT = 450;

static const float FPS = 120;
static const float DELTA_TIME = 1 / FPS;